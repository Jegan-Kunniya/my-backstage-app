import { techdocsContentRendererPlugin } from './plugin';

describe('techdocs-content-renderer', () => {
  it('should export plugin', () => {
    expect(techdocsContentRendererPlugin).toBeDefined();
  });
});
